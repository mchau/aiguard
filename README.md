# AIGuard Documentation Bundle

This bundle contains:

- `PRD.md` — full product requirements
- `SPEC.md` — technical implementation specification
- `CLAUDE.md` — build instructions for Claude Code or another AI coding agent

Suggested next step:

1. Create a new repo named `aiguard`.
2. Copy `PRD.md` and `SPEC.md` into `/docs`.
3. Copy `CLAUDE.md` into the repo root.
4. Ask Claude Code to build according to `CLAUDE.md`, one milestone at a time.
